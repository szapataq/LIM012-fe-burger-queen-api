const {
  getProducts,
  getOneProduct,
  createProduct,
  updateProduct,
  deleteProduct
} = require('../products');

describe('get all products', () => {
  it('should get users collection', (done) => {
    done();
  });
});
