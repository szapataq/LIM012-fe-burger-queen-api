const {
  getUsers, getOneUser, createUser, updateUser, deleteUser,
} = require('../users');

describe('getUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('getOneUser', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('createUser', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('updateUser', () => {
  it('should get users collection', (done) => {
    done();
  });
});

describe('DeleteUser', () => {
  it('should get users collection', (done) => {
    done();
  });
});
